<%= form_with model: @task do |f| %>
  <div class="form-group mb-4">
    <%= f.label :url, class: "form-label" %>
    <%= f.text_field :url, class: "form-control", placeholder: 'Digite a URL onde será feita a coleta de dados' %>
  </div>

  <div class="d-flex justify-content-center mt-3">
    <%= f.submit t("helpers.submit.#{action_name}", model: t('activerecord.models.task')), class: "btn btn-primary" %>
  </div>
<% end %>